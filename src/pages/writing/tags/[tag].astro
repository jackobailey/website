---
import CombinedTagPage from "@/components/tags/CombinedTagPage.astro";
import { getCombinedTagStaticPaths } from "@/lib/taggedContent";

export async function getStaticPaths() {
  return getCombinedTagStaticPaths();
}

const { entries, tag } = Astro.props;
---

<CombinedTagPage entries={entries} tag={tag} />
