---
import { siteConfig } from "@/site.config";

const pathname = Astro.url.pathname;
const navItems = [
  { href: "/", label: "Home" },
  { href: "/about/", label: "About" },
  { href: "/writing/", label: "Writing" }
];
---

<header class="pt-8">
  <div class="page-shell">
    <div class="chrome-panel flex flex-col gap-4 pb-4 sm:flex-row sm:items-center sm:justify-between">
      <a
        href="/"
        class="inline-flex items-center text-xl font-semibold tracking-[0.02em] text-ink"
        aria-label={`${siteConfig.name} home`}
      >
        <span>JACK BAILEY</span>
      </a>

      <nav aria-label="Primary navigation">
        <ul class="flex flex-wrap items-center gap-x-6 gap-y-2">
          {navItems.map((item) => {
            const isActive =
              item.href === "/" ? pathname === "/" : pathname.startsWith(item.href);

            return (
              <li>
                <a
                  href={item.href}
                  class:list={["nav-link", isActive && "nav-link-active"]}
                  aria-current={isActive ? "page" : undefined}
                >
                  {item.label}
                </a>
              </li>
            );
          })}
        </ul>
      </nav>
    </div>
  </div>
</header>
